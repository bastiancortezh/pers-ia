# Supabase Setup

## Apply schema

Run `migrations/001_initial_schema.sql` in the Supabase SQL Editor.

## Seed exercises

Run `seed.sql` in the Supabase SQL Editor after applying the schema.

## Environment variables

Copy `.env.local.example` to `.env.local` and fill in your Supabase project URL and anon key.
